name = "raman is a good boy   and "
# print(name.find("  "))
print(name.replace("  ", ""))# name will be same as it is, string are immuutable which means that you cannot change them by running functions on them